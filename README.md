# OBSAudioBlaner-updates

[OBSAudioBlaner](https://github.com/chinatsu76/OBSAudioBlaner) の起動時更新チェックが参照する `version.json` だけを公開するための専用リポジトリです。

本体のソースやリリース物はここには含みません。`OBSAudioBlaner` 側の `tools/release.js` がリリースのたびに `version.json` をここへ push します。
